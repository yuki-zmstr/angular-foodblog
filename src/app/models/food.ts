export interface Food {
  name?: string;
  img_url?: string;
  category?: string;
  // comments?: Comment[];
  comments?: string[];
  desc_en?: string;
  desc_jp?: string;
  // or just give it a list of string?
}